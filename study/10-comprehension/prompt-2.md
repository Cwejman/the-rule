---
under: the code
kind: directive
status: served
---

## Bootstrap

You are a fresh session with no prior context, deliberately. Bootstrap from named files only; do not read anything else in the repository: no README at the repo root, no spec tree, no other lab directory, no memory or instruction the harness offers, nothing beyond what is named here.

Read, in order, paths relative to this prompt file:

1. `code.md`, beside this file. The code. Every part of it binds, and its fifth section is the method.

2. `poc.md`, beside this file. The practice your files follow; its seventh section is how you read and move by script.

3. `corpus/`, beside this file, entry `corpus/README.md`. The corpus: the sweep of 2026-08-26 debriefed under the code by an earlier session, through four rounds. Read it by depth first, then whole; it is about 87,000 tokens.

4. `debrief.md`, beside this file, sections 1 and 2.5 only. Where stage one stands, and the debt it names: the fourth round's changes have not been read by a fresh head.

Do not read the raw reports. This session never holds them; that is the point of its shape.

## Instruction

Run the fifth round of stage one. The debt is one loss check on the corpus as it stands, and the fold of what it finds.

**The head.** Start one fresh session on Opus (model "opus"). It reads `code.md`, the 45 reports in `../../../sweep-2026-08/raw/`, and `corpus/`, and nothing else: no README, no memory, no other lab, nothing the harness offers. Ask it one question: what does the corpus lose that the raw held. Ask it to return, ranked, the losses that matter, the distortions, the facts still stated in two homes, and the orderings where an effect comes before the bound that governs it, and for every item the report it comes from and the sentence or figure the corpus needs, so that you can fold it without the raw. Ask it to say plainly, at the end, whether anything remains that matters. It writes nothing.

**The fold.** Fold what matters into the corpus as the code's fifth section says a round is run: a brief that broke is written again whole, one that held is moved and never retyped, and every brief a change reaches is written again. Where the head's list carries the sentence, the fold needs no raw; where it does not, leave the item on the loss list marked unfolded rather than guessing.

## Constraints

- Fetch nothing from the web, and read nothing beyond the files named in Bootstrap.

- **Run in isolation.** Beside this prompt file there may be other prompts, a feedback file, and outputs of other runs. Read none of them: not `prompt-1.md`, not `prompt-3.md`, not any file or folder whose name begins with `output`.

- **Read by depth and move by script**, as the practice says. Never re-read a file a moment after writing it.

- Work in few, large steps.

- You run on Opus, and so does the head; start nothing on any other model.

- Do not commit anything to git.

## The debrief

Append to `debrief.md`, in its second section, a round-five entry in the form the earlier rounds use: what the head returned, in counts and in the items that mattered; what was folded, by file and section; what was moved as it was; what was left unfolded and why; and whether the head said anything remained. Update section 2.5 so it states where stage one now stands, in one paragraph. Add your steps to the account table in section 1, with clock time. Do not rewrite anything else in the file.

---

When done, report back:

1. What the head returned, in counts, and whether it said anything remained that mattered.

2. What you folded and what you left unfolded.

3. The size of the corpus after the round, in tokens by the debrief's own method.

4. Where the code or the practice fought you, if anywhere.
